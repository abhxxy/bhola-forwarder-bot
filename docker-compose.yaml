services:
  bot:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - bot-data:/data
    expose:
      - "8000"
volumes:
  bot-data:
    name: bhola-forwarder-bot-data
